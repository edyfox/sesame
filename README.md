# sesame
Sesame is a security tool for Linux servers that dynamically manages network connections to hide the SSH port, minimizing exposure to exploit attempts by using ip-tables, shell scripts, and a PHP web interface.
